services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

    volumes:
      # Mount the workspace
      - ..:/workspaces/ai-transcript-app:cached

    environment:
      PYTHONUNBUFFERED: "1"

    command: sleep infinity

    # Forward ports for backend and frontend
    ports:
      - "8000:8000"
      - "3000:3000"

    # Use non-root user
    user: vscode

    depends_on:
      - ollama

  ollama:
    image: ollama/ollama:0.12.9
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

    volumes:
      # Store models in a named volume for persistence
      - ai-transcript-ollama-models:/root/.ollama
    ports:
      - "11434:11434"

volumes:
  ai-transcript-ollama-models:
